import { useEffect, useRef, useState } from 'react'
import { supabase } from '../supabase'
import { track } from '../analytics'

function MemberAccess(){
  const[email,setEmail]=useState(''),[sent,setSent]=useState(false),[loading,setLoading]=useState(false),[message,setMessage]=useState('')
  async function sendLink(e){
    e.preventDefault();setLoading(true);setMessage('')
    const clean=email.trim().toLowerCase();if(!clean){setMessage('Please enter your email address.');setLoading(false);return}
    const{error}=await supabase.auth.signInWithOtp({email:clean,options:{emailRedirectTo:window.location.origin,shouldCreateUser:true}})
    if(error){setMessage(error.message);setLoading(false);return}
    setSent(true);setLoading(false)
  }
  return <div style={{width:'min(680px,76vw)',maxWidth:680,margin:'0 auto 24px',background:'#071a33',borderRadius:14,padding:'34px 30px',color:'#fff',textAlign:'center'}}><div style={{fontSize:10,letterSpacing:'.22em',fontWeight:800,opacity:.8}}>MEMBER ACCESS</div><h3 style={{fontFamily:'Georgia,serif',fontSize:'clamp(30px,4vw,44px)',fontWeight:400,margin:'10px 0'}}>Create your free profile to join the community.</h3><p style={{color:'rgba(255,255,255,.72)',lineHeight:1.65,maxWidth:560,margin:'0 auto 22px'}}>Members can discover the full CreativeCheck community. Start with your email and we’ll send you a secure sign-in link.</p>{!sent?<form onSubmit={sendLink} style={{display:'flex',gap:10,maxWidth:540,margin:'0 auto',flexWrap:'wrap',justifyContent:'center'}}><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="Email address" style={{flex:'1 1 280px',padding:'14px 16px',borderRadius:12,border:'1px solid rgba(255,255,255,.2)',background:'#fff',color:'#111318'}}/><button disabled={loading} type="submit" style={{padding:'14px 18px',borderRadius:12,border:0,background:'#fff',color:'#071a33',fontWeight:700,cursor:'pointer'}}>{loading?'Sending…':'Continue with Email →'}</button></form>:<div style={{padding:'15px 18px',border:'1px solid rgba(255,255,255,.16)',borderRadius:12,background:'rgba(255,255,255,.06)'}}>Check your inbox and click the secure CreativeCheck link to continue.</div>}{message&&<p style={{color:'#ffb4b4',marginTop:14,fontSize:13}}>{message}</p>}</div>
}

function ProfileForm({isBusiness,session}){
  const[loading,setLoading]=useState(false),[message,setMessage]=useState(''),startedRef=useRef(false)
  function markStarted(){if(startedRef.current)return;startedRef.current=true;track('signup_started',{query:isBusiness?'business':'creative'})}
  async function handleSubmit(e){
    e.preventDefault();const form=e.currentTarget;setLoading(true);setMessage('')
    const hasPublicLink=form.website.value.trim()||form.instagram.value.trim()||form.portfolio_url.value.trim();if(!hasPublicLink){setMessage('Please add at least one public link: website, Instagram or portfolio.');setLoading(false);return}
    const profileData={full_name:isBusiness?form.company_name.value:form.full_name.value,email:session.user.email,profession:isBusiness?form.business_type.value:form.profession.value,category:isBusiness?form.industry.value:form.category.value,city:form.city.value,country:form.country.value,website:form.website.value,instagram:form.instagram.value,portfolio_url:form.portfolio_url.value,bio:form.bio.value,profile_type:isBusiness?'business':'creative',status:'pending',verified:false}
    const{error}=await supabase.from('profiles').insert([profileData])
    if(error){console.error(error);setMessage(error.message.includes('duplicate')?'A profile may already exist for this email.':'Something went wrong. Please try again.');setLoading(false);return}
    track('signup_completed',{query:isBusiness?'business':'creative'});setMessage(isBusiness?'Creative Business profile submitted successfully and pending review.':'Creative profile submitted successfully and pending review.');form.reset();startedRef.current=false;setLoading(false)
  }
  return <details className={`profile-submit-details separate-profile-form ${isBusiness?'business-profile-submit':'creative-profile-submit'}`} style={{display:'block',width:'min(680px,76vw)',maxWidth:'680px',margin:'0 auto 24px',background:'#071a33',backgroundImage:'none',borderRadius:'14px',overflow:'hidden',color:'#fff'}}><summary className="profile-submit-trigger" style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',width:'100%',boxSizing:'border-box',textAlign:'center',cursor:'pointer',color:'#fff'}}><span className="profile-submit-kicker" style={{display:'block',width:'100%',textAlign:'center',color:'#fff'}}>JOIN CREATIVECHECK</span><span className="profile-submit-title" style={{display:'block',width:'100%',textAlign:'center',color:'#fff'}}>{isBusiness?'Creative Businesses':'Creatives'} <span>↗</span></span><span className="profile-submit-copy" style={{display:'block',width:'100%',textAlign:'center',margin:'0 auto',color:'#fff'}}>{isBusiness?'Create a professional profile for your agency, studio, production company, creative brand or organisation.':'Create a professional profile for your creative practice and be discovered by the community.'}</span></summary><section className="max-w-4xl mx-auto px-8 py-14"><div className="rounded-[36px] bg-white/80 backdrop-blur-xl border border-white/80 shadow-[0_20px_60px_rgba(15,23,42,0.08)] p-8 md:p-10"><div className="mb-10"><p className="text-xs uppercase tracking-[3px] text-blue-500 font-black mb-3">{isBusiness?'CREATIVE BUSINESS PROFILE':'CREATIVE PROFILE'}</p><h2 className="text-4xl md:text-5xl font-black tracking-[-3px] text-[#0f172a] leading-[1]">Create your {isBusiness?'Creative Business':'Creative'} profile</h2><p className="mt-5 text-[16px] text-gray-500 leading-relaxed max-w-2xl">{isBusiness?'Tell the creative community who your business is, what it does and how people can find you.':'Tell the creative community who you are, what you do and where people can find your work.'}</p><p className="mt-2 text-sm text-blue-700">Signed in as {session.user.email}</p></div><form onFocus={markStarted} onSubmit={handleSubmit} className="space-y-5">{isBusiness?<><input type="text" name="company_name" required placeholder="Company / Business Name" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="text" name="business_type" required placeholder="Business Type — e.g. Production Company, Creative Agency, Studio, Brand" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="text" name="industry" required placeholder="Creative Industry / Speciality" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/></>:<><input type="text" name="full_name" required placeholder="Full Name" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="text" name="profession" required placeholder="Profession — e.g. Film Director, Photographer, Designer" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="text" name="category" required placeholder="Creative Field / Speciality" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/></>}<div className="grid md:grid-cols-2 gap-4"><input type="text" name="city" placeholder="City" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="text" name="country" placeholder="Country" className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/></div><textarea name="bio" required rows="5" placeholder={isBusiness?'Short business description — required for review':'Short professional bio — required for review'} className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a] resize-none"/><input type="url" name="website" placeholder={isBusiness?'Company Website URL':'Website URL'} className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="url" name="instagram" placeholder={isBusiness?'Business Instagram URL':'Instagram Profile URL'} className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><input type="url" name="portfolio_url" placeholder={isBusiness?'Company / Work Showcase URL':'Portfolio / Work URL'} className="w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-[#0f172a]"/><div className="rounded-2xl bg-blue-50 border border-blue-100 p-5"><p className="text-sm text-blue-700 leading-relaxed">Please add at least one public link: website, Instagram or portfolio. Incomplete profiles may not be approved.</p></div><label className="flex items-start gap-3 text-sm text-gray-500 leading-relaxed"><input type="checkbox" required className="mt-1"/><span>I confirm that submitted information is publicly shareable and may be reviewed before publication on CreativeCheck.</span></label><button type="submit" disabled={loading} className="w-full py-4 rounded-2xl bg-gradient-to-r from-[#4f46e5] via-[#2563eb] to-[#06b6d4] text-white font-semibold shadow-xl hover:scale-[1.01] transition disabled:opacity-50">{loading?'Submitting...':isBusiness?'Submit Creative Business Profile':'Submit Creative Profile'}</button>{message&&<div className="rounded-2xl bg-gray-50 border border-gray-200 px-5 py-4 text-sm text-gray-600">{message}</div>}</form></div></section></details>
}

export default function AddProfilePage({type}){
  const[session,setSession]=useState(null),[loading,setLoading]=useState(true)
  useEffect(()=>{let active=true;supabase.auth.getSession().then(({data})=>{if(active)setSession(data.session);if(active)setLoading(false)});const{data:listener}=supabase.auth.onAuthStateChange((_event,next)=>{setSession(next);setLoading(false)});return()=>{active=false;listener.subscription.unsubscribe()}},[])
  useEffect(()=>{if(session?.user?.id)supabase.rpc('claim_profile_for_current_user').catch(()=>{})},[session?.user?.id])
  if(loading)return <div style={{textAlign:'center',padding:30,color:'#777'}}>Checking member access…</div>
  if(!session)return <MemberAccess/>
  if(type==='creative')return <div style={{width:'100%'}}><ProfileForm isBusiness={false} session={session}/></div>
  if(type==='business')return <div style={{width:'100%'}}><ProfileForm isBusiness={true} session={session}/></div>
  return <div style={{width:'100%'}}><ProfileForm isBusiness={false} session={session}/><ProfileForm isBusiness={true} session={session}/></div>
}
